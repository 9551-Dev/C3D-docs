--- the scene module is used for changing things in the 3D scene and adding new geometries
-- @module[kind=modules] c3d.scene

local doc = {}

--- creates a new @{scene_object|scene object} from the given @{geometry_table_format|geometry table}
-- @treturn scene_object the created scene object
function doc.add_geometry()
end

return doc