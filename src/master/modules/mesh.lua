--- Goes along the @{raw_mesh|raw mesh object}. this lib is mainly just used
-- @module[kind=modules] c3d.mesh

local doc = {}

--- creates new empy mesh object
-- @treturn raw_mesh the created raw_mesh object
function doc.new()
end

return doc