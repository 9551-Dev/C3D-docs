--- Module to go along with the @{palette|palette} object
-- @module[kind=modules] c3d.palette(module)

local doc = {}

--- Creates a new palette object
-- @tparam table palette the palette table to make the object from
-- @treturn palette palette object
function doc.get_position(palette)
end

return doc